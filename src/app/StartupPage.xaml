<UserControl
    x:Class="Winchisel.StartupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <AutoSuggestBox x:Name="Search" PlaceholderText="Search startup entries" QueryIcon="Find" TextChanged="Search_TextChanged" />
            <Button Grid.Column="1" Content="Refresh" Click="Refresh_Click" />
        </Grid>
        <TextBlock x:Name="Status" Grid.Row="1" Text="Loading..." Style="{StaticResource CaptionTextBlockStyle}" AutomationProperties.LiveSetting="Polite" />
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <StackPanel x:Name="Items" Spacing="8" />
            </Grid>
        </ScrollViewer>
        <InfoBar x:Name="ResultBar" Grid.Row="3" IsOpen="False" IsClosable="True" AutomationProperties.LiveSetting="Assertive" />
    </Grid>
</UserControl>

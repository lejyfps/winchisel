<UserControl
    x:Class="Winchisel.ProcessesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Processes" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Inspect running processes, priority, and affinity."
                       Style="{StaticResource CaptionTextBlockStyle}" />
        </StackPanel>
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="FilterBox" SelectedIndex="0" MinWidth="180" SelectionChanged="FilterChanged">
                <ComboBoxItem Content="All" />
                <ComboBoxItem Content="Active only" />
                <ComboBoxItem Content="User processes" />
            </ComboBox>
            <Button Grid.Column="1" Content="Refresh" Click="RefreshClick" />
        </Grid>
        <Grid Grid.Row="2" RowSpacing="8">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <TextBlock x:Name="StatusText" Style="{StaticResource CaptionTextBlockStyle}" AutomationProperties.LiveSetting="Polite" />
        <ListView x:Name="ProcessList" Grid.Row="1" SelectionMode="Single" SelectionChanged="ProcessSelectionChanged">
            <ListView.Header>
                <Grid Padding="12,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>
                    <Button Content="PID" Padding="0" Click="SortPid" />
                    <Button Grid.Column="1" Content="Name" Padding="0" Click="SortName" />
                    <Button Grid.Column="2" Content="CPU" Padding="0" Click="SortCpu" />
                    <Button Grid.Column="3" Content="Priority" Padding="0" Click="SortPriority" />
                    <Button Grid.Column="4" Content="Affinity" Padding="0" Click="SortAffinity" />
                    <Button Grid.Column="5" Content="Status" Padding="0" Click="SortStatus" />
                </Grid>
            </ListView.Header>
        </ListView>
        </Grid>
    </Grid>
</UserControl>

<UserControl
    x:Class="Winchisel.LatencyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <StackPanel>
                <TextBlock Text="Latency" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="Analyze USB topology for latency issues."
                           Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
            <Button Content="Analyze" Style="{StaticResource AccentButtonStyle}"
                    HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>
        <ProgressBar Grid.Row="1" Value="0" />
        <TextBlock Grid.Row="2" Text="Ready" Style="{StaticResource CaptionTextBlockStyle}" />
        <Border Grid.Row="3" Padding="16">
            <ScrollViewer>
                <TextBlock TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
